{
  "name": "@fiap-hackathon/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared code for FIAP Hackathon microservices: Zod schemas, LLM Strategy provider, logger, error types. Copy-pasted into each service's src/shared/.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "test": "jest"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "groq-sdk": "^0.7.0",
    "pino": "^9.5.0",
    "pino-pretty": "^11.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
